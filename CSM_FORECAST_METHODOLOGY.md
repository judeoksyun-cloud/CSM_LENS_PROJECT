# CSM Lens 전망 방법론

## 1. 목적과 적용 범위

이 문서는 CSM Lens가 9개 보험사의 보유 CSM을 Base와 Worst로 전망하는 산식과 통제 기준을 정의한다. 전망치는 회사 공식 가이던스가 아니라 의사결정 보조용 시나리오이며 새 공시가 확인되면 다시 산출한다.

- 최신 실적 기준: 2026년 1분기 말
- 표시 구간: 1년(2026), 2년(2027), 3년(2028), 5년(2030), 10년(2035) 전망
- 장기 표시 구간: 내부적으로 2031~2035년을 연도별 계산하되 2035년말만 표시
- 저장 단위: 십억원
- CSM 기준: 별도재무제표·발행 보험계약·출재 재보험 제외

## 2. 기본 산식

`기말 CSM = 기시 CSM + 신계약 CSM + 이자부리 + CSM 조정 등 + CSM 상각`

CSM 상각은 음수로 저장한다. 2026년은 공시된 1분기 실적과 2~4분기 전망을 합산한다. 2027년 이후는 전년도 기말 CSM을 다음 연도 기시 CSM으로 연결한다. 모든 표시 시점과 내부 장기 계산 시점에서 Movement 산식을 다시 검산한다.

## 3. 항목별 전망

### 신계약 CSM

최근 실적, 전년 동기, 2025년 분기 계절성과 최근 3개 연도말 신계약 CSM 추이를 1차 기준으로 사용한다. 외부 정성 보정은 증권사 애널리스트 리포트만 허용한다. 기사, 언론 보도, 회사 보도자료, 일반 산업전망 자료는 전망 입력에서 제외한다.

2027~2030년 신계약 증가율은 2023~2025년 증가율을 최근 연도 65%, 직전 연도 35%로 가중한 뒤 연 -3%~+5% 범위로 제한한다. 2031~2035년은 이 증가율의 절반 수준으로 수렴시키고 연 -1%~+2% 범위로 제한한다.

### CSM 조정 등

2026년 Base는 최근 조정의 정상 구간과 연말 집중도를 고려해 회사별 잔여 조정을 설정한다. Q2·Q3·Q4 배분은 20%·20%·60%를 사용한다. 2027년 이후 Base 조정은 2026년 연간 조정의 기시 CSM 대비율을 적용한다.

### 이자부리와 CSM 상각

정성 판단 없이 최근 실적 비율로 자동 산출한다.

`이자부리율 = 이자부리 ÷ (기시 CSM + 신계약 CSM)`

`CSM 상각률 = |CSM 상각| ÷ (기시 CSM + 신계약 CSM)`

최근 4개 분기(2025 Q2~2026 Q1)에 10%·20%·30%·40% 가중치를 적용한다. 각 전망 분기와 연도에서 전망 신계약 CSM을 포함한 동일 분모에 비율을 다시 적용한다. 교보생명은 과거 이자부리와 조정 사이의 큰 재분류 변동 때문에 정상화한 0.7%를 사용하고 예외 여부를 데이터에 기록한다.

## 4. Base와 담당자 앵커

담당자 입력값이 없으면 과거 Movement와 허용된 애널리스트 근거로 계산한 기말 CSM을 모델 Base로 사용한다. 담당자가 특정 연도말 CSM을 입력하면 해당 값을 Base 목표로 고정하고 다음 순서로 보정한다.

1. 실적 추이와 증권사 애널리스트 근거로 신계약 CSM을 설정한다.
2. 이자부리와 상각을 최근 실적 비율로 계산한다.
3. 목표 기말 CSM에 맞는 CSM 조정 등을 잔여값으로 계산한다.
4. 역산 조정값이 과거 정상 범위를 벗어나면 경고하고 입력값을 재검토한다.

현재 전망에는 담당자 앵커가 없으며 모두 모델 Base이다.

## 5. Worst: 단일 20% 악화 원칙

Worst는 회사마다 별도의 절대 조정금액을 임의로 설정하지 않는다. 모든 회사와 모든 전망연도에 같은 규칙을 적용한다.

- 신계약 CSM: Base 대비 20% 감소
- CSM 조정이 음수인 경우: Base 부담을 20% 확대 (`Base 조정 × 1.20`)
- CSM 조정이 양수인 경우: Base 이익을 20% 축소 (`Base 조정 × 0.80`)
- 이자부리·상각: 스트레스된 신계약과 CSM 규모에 기존 기계 산식 비율을 다시 적용

2026년 1분기는 이미 확정된 실적이므로 스트레스를 적용하지 않고, 2~4분기 전망 구간에만 20% 악화 규칙을 적용한다.

## 6. 장기 전망

2026~2030년은 내부적으로 매년 계산하되 대시보드에는 임원보고 관점의 1~3년과 5년 전망만 표시한다. 2031~2035년 역시 단기 추세가 완만하게 수렴한다고 가정해 매년 Movement를 계산하고 10년 전망인 2035년말만 표시한다. 2029년과 2031~2034년의 기말 CSM은 다음 연도 기시로 연결되므로 표시하지 않는 연도도 계산에서 생략하지 않는다.

### 임원보고용 구간별 해석

- 1~3년: 최근 분기 실적, 계절성 및 증권사 애널리스트 방향성을 반영한 실행 가능성이 가장 높은 단기 경로
- 5년: 과거 신계약 증가율과 CSM 조정률이 정상 범위에서 이어진다고 가정한 중기 체력
- 10년: 단기 성장률을 절반 수준으로 수렴시키고 모든 중간연도 Movement를 누적한 장기 종착점

## 7. 외부자료 정책

- 허용: 증권사 소속 애널리스트가 발간한 기업·산업 리포트 PDF
- 제외: 뉴스 기사, 포털 재전송 기사, 회사 보도자료, 컨퍼런스콜 기사, 일반 연구기관 전망
- 회사별 직접 리포트가 없으면 `외부 보정 없음`으로 처리하고 실적 추이만 사용한다.
- 업종 리포트는 방향성 보정에만 사용하고 회사 CSM의 직접 목표값으로 사용하지 않는다.
- 사용한 리포트 제목, 링크와 반영 항목은 회사별 상세 화면과 전망 JSON에 저장한다.

## 8. 검증과 통제

- 모든 연도와 시나리오에서 Movement 산식이 일치해야 한다.
- 모든 외부 출처의 `type`은 `sell_side`여야 한다.
- Worst 신계약·조정 스트레스는 모두 20%여야 한다.
- Base보다 Worst 기말 CSM이 낮아야 한다.
- 표시 시점인 2026·2027·2028·2030·2035년말이 모두 존재해야 한다.
- 실적 확정 후 Base·Worst 오차와 Movement 항목별 오차를 백테스트한다.

## 9. 산출 결과

단위는 십억원이다.

| 회사 | 최근 실적 | 2026 Base | 2026 Worst | 2030 Base | 2030 Worst | 2035 Base | 2035 Worst | 신뢰도 |
|---|---:|---:|---:|---:|---:|---:|---:|:---:|
| 삼성생명 | 13,647 | 14,404 | 13,762 | 16,926 | 13,448 | 17,740 | 11,693 | 중상 |
| 한화생명 | 8,921 | 9,497 | 8,984 | 10,927 | 7,905 | 11,168 | 5,623 | 중상 |
| 교보생명 | 6,687 | 7,058 | 6,746 | 8,176 | 6,419 | 8,496 | 5,403 | 중 |
| 신한라이프 | 7,610 | 7,921 | 7,573 | 9,929 | 7,855 | 12,106 | 7,993 | 중 |
| 삼성화재 | 14,469 | 14,922 | 14,339 | 16,451 | 13,561 | 16,796 | 11,937 | 중 |
| 메리츠화재 | 11,291 | 11,556 | 11,206 | 13,760 | 11,716 | 16,449 | 12,542 | 중상 |
| DB손해보험 | 12,822 | 13,173 | 12,507 | 15,999 | 12,667 | 17,821 | 11,799 | 중 |
| 현대해상 | 9,248 | 9,507 | 9,048 | 11,854 | 9,214 | 14,351 | 9,212 | 중 |
| KB손해보험 | 9,478 | 9,920 | 9,560 | 11,380 | 9,424 | 11,982 | 8,501 | 중하 |

회사별 증권사 원문 링크, 반영 항목, 단기 연도별 Movement와 장기 종착점은 `external-data/csm-forecast-2026.json`에 버전과 함께 보존한다.
