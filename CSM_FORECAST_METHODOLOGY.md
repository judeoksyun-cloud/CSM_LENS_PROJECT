# CSM Lens 전망 방법론

## 1. 목적과 적용 범위

이 문서는 CSM Lens가 9개 보험사의 보유 CSM Base와 Worst를 산출하는 방식과 통제 기준을 정의한다. 삼성생명에는 공통 모델 위에 Driver Forecast 파일럿을 적용한다. 전망치는 회사 공식 가이던스가 아니라 의사결정 보조용 시나리오이며 새 공시가 확인되면 다시 산출한다.

- 최신 실적 기준: 2026년 1분기 말
- 표시 구간: 1년(2026), 2년(2027), 3년(2028), 5년(2030), 10년(2035) 전망
- 장기 표시 구간: 내부적으로 2031~2035년을 연도별 계산하되 2035년말만 표시
- 저장 단위: 십억원
- CSM 기준: 별도재무제표·발행 보험계약·출재 재보험 제외

## 2. 기본 산식

`독립 모델 기말 CSM = 기시 CSM + 신계약 CSM + 이자부리 + CSM 조정 등 + CSM 상각`

`경영계획 Base = 기시 CSM + 신계약 CSM + 이자부리 + CSM 조정 등(목표 정합화 조정 포함) + CSM 상각`

CSM 상각은 음수로 저장한다. 별도 경영목표가 있는 경우 독립 모델과 목표의 차이는 `CSM 조정 등`에 포함한다. 원래 모델 조정값은 `adjustmentBeforeTargetOverlay`, 목표 정합화분은 `targetAdjustmentOverlay`로 함께 저장해 감사 추적성을 유지한다. 2026년은 공시된 1분기 실적과 2~4분기 전망을 합산한다. 2027년 이후는 전년도 Base 기말 CSM을 다음 연도 기시 CSM으로 연결하되 2026년 목표 정합화분은 반복 조정률에서 제외한다.

## 3. 항목별 전망

### 신계약 CSM

Q1 이후 잔여 신계약 CSM은 전년도 Q2~Q4 실적을 출발점으로 삼는다. 당해 Q1 전년동기 성장률 65%와 직전 잔여기간 성장률 35%를 섞고 신호를 50% 축소해 과잉 추종을 막는다. 적용 성장 신호는 -20%~+20%로 제한하며, Q2~Q4 배분은 최근 최대 3개년 실제 계절성의 평균을 사용한다.

2027~2030년 신계약 증가율은 2023~2025년 증가율을 최근 연도 65%, 직전 연도 35%로 가중한 뒤 연 -3%~+5% 범위로 제한한다. 2031~2035년은 이 증가율의 절반 수준으로 수렴시키고 연 -1%~+2% 범위로 제한한다.

### CSM 조정 등

2026년 Base 잔여 조정은 최근 최대 3개년의 `Q2~Q4 조정 합계 ÷ Q1 기말 CSM` 비율 중 중앙값을 적용한다. Q2·Q3·Q4 배분은 같은 기간 실제 조정 절대금액의 계절성 평균을 사용한다. 2027년 이후 Base 조정은 2026년 연간 조정의 기시 CSM 대비율을 적용한다.

### 이자부리와 CSM 상각

정성 판단 없이 최근 실적 비율로 자동 산출한다.

`이자부리율 = 이자부리 ÷ (기시 CSM + 신계약 CSM)`

`CSM 상각률 = |CSM 상각| ÷ (기시 CSM + 신계약 CSM)`

각 예측 기준시점 직전 4개 분기에 10%·20%·30%·40% 가중치를 적용한다. 각 전망 분기와 연도에서 전망 신계약 CSM을 포함한 동일 분모에 비율을 다시 적용한다. 교보생명은 과거 이자부리와 조정 사이의 큰 재분류 변동 때문에 정상화한 0.7%를 사용하고 예외 여부를 데이터에 기록한다.

### 삼성생명 Driver Forecast 파일럿

삼성생명은 2026년 1분기 실제값을 고정하고 다음 세 개의 드라이버를 별도 브리지로 계산한다.

2026년말 경영계획 Base는 사용자 제공 경영목표 13.5조원을 적용한다. Driver 모델의 독립 산출값과 차이는 2026년 `CSM 조정 등`에 포함하고, 상세 설명에 목표 정합화분이 포함됐음을 명시한다. 이 차이는 2027년 이후 반복 조정률에는 포함하지 않는다. 2027년부터는 13.5조원을 기시 CSM으로 삼아 정상화 Movement를 다시 계산한다. 화면에는 목표 입력 방식을 `경영 입력·담당자 별도 기입`으로 표시하며 독립 모델 전망을 화면과 JSON에 병렬 보존한다.

- 신계약 CSM: 공통 계절성 모델의 잔여 신계약 60%와 과거 3개년 Q1의 연간 신계약 CSM 비중 중앙값으로 환산한 2026년 Q1 런레이트 40%를 결합한다. FC 증가와 건강보험 중심 판매는 40% 런레이트 가중치를 유지하는 방향성 근거이며 별도 임의 가산율로 쓰지 않는다.
- CSM 상각: 최근 4개 분기의 `|상각| ÷ (기시 CSM + 신계약 CSM)` 가중평균을 coverage-unit 서비스 제공률 대용치로 사용한다. 상품군별 coverage unit이 정규화되면 이 대용치를 교체한다.
- CSM 조정: 과거 각 연도 Q2~Q4에서 절대금액이 가장 큰 한 분기를 사건성·연말 가정점검분으로 분리하고 나머지 합계의 중앙값을 반복 경험조정으로 본다. 공통 모델의 잔여 조정과 반복분의 차이는 연말 가정점검 준비분으로 남긴다.

경영진 화면의 각 Movement에는 `변동 이유`와 별도로 `근거·출처`를 연결한다. 근거는 확정 공시 실적, 경영진 실적발표 설명, 모델 산출식과 적용 한계로 구분하며 모든 항목은 `sourceId`로 원문 출처 레지스트리를 참조한다. 정성 설명만 있고 연결 가능한 원문이나 산식이 없는 경우 근거 항목으로 표시하지 않는다.

2026년 1분기에 발표된 11.4배 신계약 지표는 2025년 APE 대비 CSM 비율과 분모 정의가 같다고 검증되지 않았다. 따라서 APE나 신계약 CSM을 역산하는 입력으로 쓰지 않고 참고 지표로만 기록한다.

Base는 독립 모델 또는 별도 공개된 경영계획 경로다. Worst는 9개사 2024·2025년의 Q1·Q2·Q3 시점에서 연말을 예측한 총 54개 롤링 검증 결과로 산출한다. 회사별 6개 표본과 생보·손보 업권별 표본의 80백분위 오차를 각각 50% 혼합해 회사별 하방 스트레스를 만든다. 이를 정밀한 확률 예측이나 실제 최악값으로 해석하지 않는다.

원인별 스트레스도 같은 Movement 산식으로 재계산한다.

- 판매 둔화: 신계약 CSM만 백테스트 하방폭만큼 감소
- 수익성 압박: APE를 직접 추정하지 않고 신계약 CSM 전환효율 하락을 신계약 하방폭의 절반으로 대용
- 장래손해율·해지·비용 부담: 신계약은 유지하고 조정 항목에 기시 CSM 대비 조정 오차 80백분위를 추가한다. 장래손해율 가정 상승, 해지율 상승, 사업비 가정 악화를 CSM 조정 하방으로 묶어 반영하며, 가정별 원자료가 정규화되기 전까지 세부 요인별 금액은 임의 배분하지 않는다.
- 복합 스트레스: 판매 둔화와 장래손해율·해지·비용 부담을 동시에 적용하며 Worst와 같은 종착점

## 4. 독립 모델, 경영계획 Base와 애널리스트 오버레이

독립 모델은 과거 Movement와 계절성으로 먼저 산출한다. 회사별 직접 증권사 근거가 있으면 잔여 신계약과 조정에 25%만 반영한다. 별도 경영목표가 있으면 `경영계획 Base`를 만들고 모델과의 차이를 `CSM 조정 등`에 포함한다. 독립 모델, 목표 정합화 조정액과 입력 검증 상태는 JSON에 각각 보존한다.

Base의 기본값은 과거 Movement만으로 계산한 데이터 모델이다. 회사별 직접 증권사 근거가 있는 경우에만 해당 정성 판단을 잔여 신계약과 조정에 각각 25% 반영하고, 나머지 75%는 데이터 모델을 유지한다. 업종 공통 리포트만 있는 회사는 오버레이를 적용하지 않는다. 모델 산출값, 정성 입력값, 가중치, 최종 입력값을 JSON에 각각 기록한다.

## 5. Worst: 백테스트 오차 기반 원칙

2024년과 2025년 각 Q1·Q2·Q3 시점에서 해당 연말을 예측하는 롤링 백테스트를 9개사에 수행한다. 회사별 6건, 전체 54건이다. 신계약과 조정 오차는 회사별 80백분위와 해당 업권 80백분위를 각각 50% 혼합한다.

- 신계약 CSM: 잔여 신계약 예측의 회사·업권 혼합 절대비율오차만큼 Base에서 감소
- CSM 조정: 잔여 조정 오차를 기준시점 CSM으로 나눈 회사·업권 혼합 비율만큼 추가 부담 반영
- 이자부리·상각: 스트레스된 신계약과 CSM 규모에 기존 기계 산식 비율을 다시 적용

2026년 1분기는 이미 확정된 실적이므로 스트레스를 적용하지 않고 2~4분기 전망 구간에만 오차 기반 하방 규칙을 적용한다. 적용률은 회사마다 다르며 JSON의 `horizon.assumptions`에 저장한다.

## 6. 장기 전망

2026~2030년은 내부적으로 매년 Movement를 계산한다. 대시보드에는 1년·2년·3년·5년 예상의 Base와 Worst를 표시한다. 2030년 Base는 10년 예상의 출발점으로 사용한다.

2035년은 `10년 예상`으로 표시하고 다른 전망 시점과 동일하게 Base와 Worst를 사용한다. 2030년 Base와 Worst를 각각 출발점으로 2031~2035년 모든 중간연도 Movement를 계산하며, 별도의 상·중·하 장기 시나리오는 사용하지 않는다.

### 임원보고용 구간별 해석

- 1~3년: 최근 분기 실적, 계절성 및 증권사 애널리스트 방향성을 반영한 실행 가능성이 가장 높은 단기 경로
- 5년 예상: 최근 실적과 현재 Movement 가정을 연도별로 연결한 2030년 Base와 Worst
- 10년 예상: 2030년 Base와 Worst를 각각 출발점으로 산출한 2035년 Base와 Worst

화면의 검증 수준은 `1년: 제한적 검증`, `2~3년: 모델 경로`, `5년: 시나리오`, `10년: 장기 시나리오`로 구분한다. 장기 값은 소수점 한 자리까지만 표시해 정밀 예측처럼 보이지 않게 한다.

## 7. 외부자료 정책

- 허용: 증권사 소속 애널리스트가 발간한 기업·산업 리포트 PDF
- 제외: 뉴스 기사, 포털 재전송 기사, 회사 보도자료, 컨퍼런스콜 기사, 일반 연구기관 전망
- 회사별 직접 리포트가 없으면 `외부 보정 없음`으로 처리하고 실적 추이만 사용한다.
- 업종 리포트는 방향성 보정에만 사용하고 회사 CSM의 직접 목표값으로 사용하지 않는다.
- 사용한 리포트 제목, 링크와 반영 항목은 회사별 상세 화면과 전망 JSON에 저장한다.
- 삼성생명 Driver Forecast는 공식 공시와 실적발표 발언을 현재 드라이버의 사실 확인에 사용할 수 있다. 이 자료의 수치를 외부 목표값으로 입력하지 않으며 공통 애널리스트 오버레이와 별도 필드에 저장한다.

## 8. 검증과 통제

- 모든 연도와 시나리오에서 Movement 산식이 일치해야 한다.
- 확정 사실은 DART·회사 IR, 외부 전망은 회사별 증권사 리포트, 모델 추정은 방법론 문서로 구분한다.
- 백테스트는 예측시점 이후 데이터를 입력으로 사용하지 않는 롤링 방식이어야 한다.
- Worst 스트레스는 회사별 6개 시점과 업권별 시점의 80백분위 50:50 혼합값과 일치해야 한다.
- Base보다 Worst 기말 CSM이 낮아야 한다.
- 표시 시점인 2026·2027·2028·2030·2035년말의 Base와 Worst가 존재해야 한다.
- `longTermScenarios`가 데이터 계약에 존재하지 않아야 한다.
- 2030~2035 CAGR은 2030년 Base와 각 정상화 경로의 2035년말 CSM으로 재계산한 값과 일치해야 한다.
- 회사별 백테스트 MAPE·MAE·Bias와 6개 오차 표본을 JSON에 보존한다. 표본이 8개 미만이면 `검증 제한`으로 표시한다.
- 삼성생명 목표 정합화 전 조정값과 목표 정합화 조정액은 별도 필드로 보존하며, 합산한 `CSM 조정 등`과 전체 Movement가 경영계획 Base에 일치해야 한다.
- 삼성생명 Driver Base와 공통 Base, Driver Worst와 공통 Worst는 같은 값을 사용해 화면·AI·Agent Run의 숫자가 갈라지지 않게 한다.
- 분모 정의가 검증되지 않은 11.4배 지표는 APE 또는 신계약 CSM 계산 입력으로 사용하지 않는다.

## 9. 산출 결과와 단일 진실 원천

숫자 표를 방법론 문서에 중복 기재하지 않는다. 회사별 독립 모델, 경영계획 Base, Worst, 증권사 원문, 반영 항목, 단기 연도별 Movement와 2035년 Base·Worst는 생성기에서 함께 만드는 `external-data/csm-forecast-2026.json`과 `csm-prototype/forecast-data.generated.js`에 동일 버전으로 보존한다. 대시보드·AI·Agent Run은 이 생성 계약만 읽는다.
